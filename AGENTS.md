## Default working rules

- Do not run `npm run build` automatically.
- Do not run build commands at the end of a task unless I explicitly ask for them.
- Only run the smallest relevant verification step needed.
- Prefer tests, lint, or typecheck over full builds when validating changes.
- Before running any expensive command, explain why it is needed.